<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash Screen Theme (Android 12+) -->
    <style name="Theme.App.Starting" parent="Theme.AppCompat">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/yellow_ffa216</item>
        <item name="postSplashScreenTheme">@style/Theme.Paradisonesia</item>
    </style>

</resources>